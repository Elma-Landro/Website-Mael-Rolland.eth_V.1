# Patch scope — SourceQuote effective patch (phase 1)

This patch is the **actual content payload** to close the most obvious citation gaps.

## What it covers
- `II.3.1`
- `III.2.2`
- all current `CRITICAL_ZERO` subsections in `intro_A`, `intro_B`, `intro_C`

## Strategy
- use **one quote when it legitimately anchors both a parent section and one of its subsections**
- avoid artificial multiplication of quotes
- prioritize structurally strong, paginated passages

## Result
- every currently uncovered section in this patch gets at least one **direct** `SourceQuote` anchor
- the same quotes can then feed both the thesis reader and graph story modes

## Important
This is not another audit. It is a **content-complete patch payload** for an implementation agent.