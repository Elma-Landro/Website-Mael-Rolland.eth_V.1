# docs/audits/data — données probantes des audits

Ces CSV/JSON sont les **données probantes** des audits de `docs/audits/`.
Chaque fichier se lit avec l'audit qui l'a produit — jamais seul : les
définitions, baselines simulées et mises en garde vivent dans l'audit, pas
dans le CSV.

| Fichier | Audit d'origine |
|---|---|
| `panel-policy-impact-v110.csv` | `grc20-panel-policy-lab-v1.md` (baseline = simulation fidèle du lecteur : parentes agrégées, tri stable sans bris d'égalité) |
| `poids-ancrage-diagnostic-v110.csv` | `grc20-poids-ancrage-v2.md` |
| `anchoring-baseline.json` | `scripts/check_anchoring.py` (régressions acceptées, motivées) |
| `bibliographie-reconciliation-v109.csv` | `grc20-bibliographie-reconciliation-v1.md` |
| `bibliographie-18-fiches-douteuses-v1.csv` | `grc20-bibliographie-reconciliation-lab-v1.md` (tri alphabétique stable, aucun horodatage ; contient **22** fiches malgré le « 18 » du nom, hérité du périmètre du patch_18 — 18 douteuses + 4 homonymie/multi-auteurs) |
| `bibliographie-doublons-familles-v1.csv` | `grc20-bibliographie-reconciliation-lab-v1.md` (tri par famille_id ; cible de fusion ≠ libellé final — lire § 3) |
| `bibliographie-entrees-sans-noeud-v1.csv` | `grc20-bibliographie-reconciliation-lab-v1.md` (tri par patronyme_annee ; lignes NAKAMOTO 2010F et ORLEAN 2002 corrigées par la revue hostile du 2026-08-07) |
| `bibliographie-parasites-et-jamais-cites-v1.csv` | `grc20-bibliographie-reconciliation-lab-v1.md` (tri par nom) |
| `catalogue-evenements-v2-fusion.csv`, `chronologie-domaines-decodee.csv`, `chronology-events-*.csv`, `added-events-from-chronology.csv`, `rejected-or-ambiguous-chronology-events.csv`, `dates-verification-externe.csv` | `grc20-chronologie-domaines-decodage.md`, `grc20-add-missing-events/`, `grc20-dedup-events-audit-v1.md` |
| `domaines-divergences.csv` | `grc20-domaines-developpement-v1.md` |
| `doublons-verifies.csv` | `grc20-dedup-events-audit-v1.md` |
| `maturation-phase-wiring.csv` | `grc20-maturation-phase-wiring.md` |
| `section-migration.csv`, `section-tree.csv` | `grc20-section-migration-chapitre-I.md`, `grc20-section-migration-chapitres-II-III.md` |
| `identity-debt-fr-en-v1.csv` | `grc20-sourcequote-migration-verification-v1.md` (§ découvertes annexes — relevé des 25 couples de fiches désignant un même référent apparent sous deux libellés, de part et d'autre de la frontière FR/EN ; une ligne par couple, tri par `paire_id` (ordre alphabétique de `name_fr`), types et degrés recomptés sur v110). **Ce relevé est une dette consignée, pas un plan de fusion** : aucune des 25 paires n'est arbitrée, la colonne `arbitrage_requis` pose une question et n'y répond pas. Les 5 lignes `nature=conflit-de-types` (dont `Pools de minage` / `Mining pools`, ActorGroup contre InfrastructureEvent) exigent un **arbitrage sémantique de l'auteur** avant toute action — jamais de fusion automatique. Les lignes `voisinage-non-doublon` et `indetermine` sont des refus de conclure, pas des candidats faibles. Ne jamais fusionner sur la seule foi de `preuve_correspondance` : c'est une trace de libellé, pas une preuve de référent. |
| `entity-alias-table-v1.csv` | `grc20-sourcequote-migration-verification-v1.md` (table d'alias EN→FR et variantes de libellés sur v110, une ligne par couple entité/alias, tri par `canonical_name` puis `alias`). **Colonnes exigées par `scripts/resolve_entity_names.py`** : `entity_id`, `alias` et `confidence` — leur absence fait échouer le résolveur à l'invocation. Il lit en outre `notes` (une note d'interdiction y écarte la ligne, même graduée haute ou moyenne — voir ci-dessous) et `alias_kind` (rapporté, non décisionnel). Toute réécriture de la table doit conserver ces cinq colonnes. **Une table d'alias n'est pas une autorisation de fusion** : elle sert à empêcher qu'un résolveur *recrée* en double une entité déjà présente sous un autre libellé — la décision d'identité (fusionner, renommer, créer) reste à l'auteur. Les alias `confidence=basse` ne doivent **jamais** servir à une résolution automatique : ils couvrent les chaînes ambiguës (un même alias pour plusieurs entités), les collisions (`collision_with` non vide — la chaîne est déjà le nom d'une autre fiche) et les traductions interprétatives. |
| `chronology-date-inventory-v111.csv` | `grc20-chronology-dates-references-lab-v1.md` (inventaire mécanique, une ligne par couple entité×clé datée, 948 lignes, tri stable `(entity_id, attribute_key)` ; **le seul des cinq CSV du chantier à être intégralement généré par un script** (celui d'adossement l'est pour ses trois dernières colonnes seulement) — régénérable et vérifiable par `scripts/audit_chronology_dates.py --csv` / `--check`, date de référence **gelée** au 2026-08-08. Attention : le script dérive le nom du CSV du graphe le plus récent, donc `--check` cherchera `…-v112.csv` le jour où une v112 existera, et échouera faute de le trouver). **Deux des 24 clés ne portent pas de date** : `dateSource` est une référence bibliographique, `dateAuthority` un niveau de preuve — elles pèsent 144 des 158 `non_parsable`, si bien que le taux réel de valeurs illisibles est 1,5 %, pas 17 %. `normalized_iso` **s'arrête volontairement à l'année** sur les 30 valeurs `dmy_ambiguous=oui` (28 sur `date`, 2 sur `dateRange`) : ne jamais compléter ce champ par convention sans arbitrage de l'auteur. Le § 3 de l'audit établit que les **26 valeurs décidables sont JJ/MM à 26 sur 26** — la convention dominante est donc JJ/MM — mais qu'une valeur, `ca278d21` (Heartbleed), se lit MM/JJ d'après une source primaire. **Arbitré le 2026-08-09** : convention JJ/MM confirmée pour les 28 ambiguës, et Heartbleed traitée comme cellule fausse de la figure (correction portée par `patch_candidate_chronology_dates_v1.json`, non appliqué). Confirmer une lecture n'autorise pas à réécrire les 28 : elles restent telles quelles. `description_conflict=oui` est un **signalement**, pas une erreur : une description cite volontiers l'année d'une source, non celle du fait. |
| `chronology-thesis-crosscheck-v111.csv` | `grc20-chronology-dates-references-lab-v1.md` — **construit à la main, aucun générateur, non rejouable** (260 dates confrontées au Markdown de la thèse, 222 citations verbatim extraites programmatiquement puis revérifiées à la ligne). `figure_non_verifiable_dans_le_MD` (51 lignes) **n'est ni une confirmation ni une réfutation** : la source est une image absente du dépôt — ne jamais le compter comme un défaut du graphe. `absent_du_texte` recouvre deux cas que la colonne `notes` distingue toujours (entité introuvable / entité citée mais date absente). Trois des 5 `desaccord_these_probablement_fausse` sont des **contradictions internes à la thèse**, pas des erreurs du graphe. `assets/MD/` reste intouché : une erreur y est signalée, jamais corrigée. |
| `chronology-external-verification-v111.csv` | `grc20-chronology-dates-references-lab-v1.md` — **construit à la main, aucun générateur, non rejouable** ; ses `entity_id`, d'abord versés **tronqués à 8 caractères**, ont été remplacés par la forme complète le 2026-08-09 après vérification que les 40 préfixes résolvent chacun vers exactement une entité — un identifiant tronqué casse les jointures **sans lever d'erreur** (40 dates à fort enjeu, lot sélectionné sur quatre critères déclarés ; 28 URL toutes réellement récupérées). **Les 24 `indice_unique` sont une contrainte d'accès, pas un défaut de recoupement** : seuls `github.com`, `raw.githubusercontent.com` et `gitlab.com` sont joignables. La date de l'attaque The DAO reste `non_verifie` — connue de mémoire, et une date de mémoire ne s'inscrit pas. **`ethereum-org-website/.../ethereum-forks/index.md` ne doit jamais être cité comme source de dates** : il ne contient aucune date, seulement des balises `<NetworkUpgradeSummary />`, et une première lecture en a rendu un tableau plausible et faux. |
| `chronology-event-identity-debt-v111.csv` | `grc20-chronology-dates-references-lab-v1.md` — **construit à la main, aucun générateur, non rejouable ; le périmètre de 259 entités et les 881 paires candidates ne sont définis nulle part et n'ont pas pu être reconstruits par la revue hostile — ordres de grandeur, non mesures** (259 entités du périmètre événementiel, 881 paires candidates, 50 instruites en 31 familles, une ligne par paire avec `famille_id` partagé). **Aucune canonique n'est désignée et aucun retypage proposé** : les colonnes `degre_a`/`degre_b` sont là pour que l'auteur voie que la canonique déjà retenue par `patch_10` est la moins reliée (piège C5). `two_distinct_events_possible` **n'est pas un doublon faible** : deux dates y sont vraies parce qu'elles décrivent deux actes. Contredit un verdict antérieur : `doublons-verifies.csv` classe la paire BitcoinTalk en `REJET_AUTO` pour « dates contradictoires » alors que l'écart *est* l'indice du doublon. |
| `chronology-reference-support-v111.csv` | `grc20-chronology-dates-references-lab-v1.md` — **relevé construit à la main**, mais ses trois dernières colonnes (`regime_de_preuve`, `confirme_par_these`, `motif_regime`) sont **générées et vérifiables** par `scripts/classify_date_evidence.py --write` / `--check`, en application de l'arbitrage 10 du 2026-08-09. **`regime_de_preuve` ne dit rien de la véracité** : il classe la nature de l'appui, pas sa validité — une date `these_verbatim` peut être fausse, le chantier en a trouvé. `infere` n'est pas un reproche mais l'aveu qu'on ne sait pas d'où vient la date. La précédence `source_primaire > these_verbatim > approximatif > infere` est un **choix déclaré**, pas une évidence ; c'est pourquoi `confirme_par_these` porte l'information séparément et ne disparaît jamais derrière elle (608 lignes, deux volets : adossement de 239 dates événementielles, puis années de 369 `Reference` contre `07_bibliographie.md`). **Ce relevé mesure l'adossement, jamais la véracité** — une date sourcée n'est pas une date juste, et `non_adosse` signifie « le graphe ne déclare pas de source », **non « la date ne repose sur rien »** : 26 des 86 `non_adosse` sont confirmées verbatim par le texte de la thèse dans le CSV frère. `entree_bibliographique_introuvable` (13) est une **non-résolution**, pas un défaut : elle ne dit pas si l'entrée manque au Markdown ou si la fiche est mal formée. Périme une affirmation de v97 : `dateAuthority` porte désormais **trois** valeurs (37 `TIMELINE_FIGURE`, 11 `AUTHOR_VERIFIED`, 1 `EXTERNAL_VERIFICATION`) — le 37 survit comme sous-ensemble, ce qui rend l'ancienne formule trompeuse et non simplement périmée. |
| `patch-application-queue-v113.csv` | `grc20-patch-application-queue-v112.md` (inventaire des **31** artefacts de patch du dépôt — dont `new_relations_patch.json`, 11 884 ops, que le glob initial ratait parce que son nom ne *commence* pas par « patch » — une ligne par fichier, tri par chemin ; généré et vérifiable par `scripts/build_patch_queue_inventory.py --csv` / `--check`). **Le nom du fichier suit le graphe de référence** — une file décrit un état, et un CSV nommé v112 qui décrirait v113 mentirait en silence. **Le statut ne vient JAMAIS de ce que le patch déclare** : il est mesuré dans le graphe courant, op par op. C'est ce qui permet de classer `already_applied` deux patchs qui portent encore, par convention de CI, la mention `CANDIDATE — NOT APPLIED` — `patch_candidate_section_page_start_v1.json` (16/16 réalisées, `make_v111`) et `patch_candidate_chronology_dates_v1.json` (2/2, `make_v112`). `indetermine` **n'est pas un feu vert** : c'est un statut NON établi, soit état mixte d'un patch historique — un rejeu naïf y échouerait sur des cibles mortes — soit dialecte illisible. Ce CSV **remplace de fait** `candidate-patch-inventory-v1.csv`, resté un instantané de v110 qui ignore les deux applications depuis. |
| `patch-queue-governance-cases-v113.csv` | `grc20-patch-queue-governance-lab-v1.md` (31 artefacts, une ligne par fichier, tri par chemin ; généré et vérifiable par `scripts/build_patch_queue_governance.py --csv` / `--check`). **Répond à « comment le dépôt doit-il traiter cet artefact ? », pas à « où en est-il ? »** — pour cela, lire `patch-application-queue-v113.csv`. `recommended_governance_status` est une **proposition non appliquée** : rien dans le dépôt ne s'y conforme tant que l'auteur n'a pas arbitré. Le chiffre structurant : **un seul artefact sur 31 est encore applicable**, et il est gelé par arbitrage — c'est l'argument contre une CI stricte. `archive_partial` (7 lignes) **interdit le mot `already_applied`** : les ops lisibles sont faites mais des cibles sont mortes, un rejeu écrirait à côté. |

Mises en garde permanentes :

- **`direct_anchor_count` est une mesure pauvre** — occurrences littérales
  du nom en limites de mots ; elle sous-compte les noms forgés, traduits ou
  bilingues. Ne jamais la lire seule, toujours croiser avec
  `snippet_status` (grc20-poids-ancrage-v2.md § 5-6).
- **`snippet_status` n'est pas un certificat** — il atteste qu'un extrait
  de la carte contient le nom ou sa base de citation, rien de plus ; les
  clés de section des extraits sont elles-mêmes partiellement périmées.
- **Les baselines simulées sont documentées dans leur audit respectif** —
  par exemple, la baseline `legacy` de `panel-policy-impact-v110.csv`
  reproduit le pipeline réel de `lecteur.html` (agrégation des parentes,
  tri stable) et ses limites (ex æquo flottants inter-langages) sont
  déclarées dans `grc20-panel-policy-lab-v1.md` § 5.
